<%= form_for(@follow) do |f| %>
  <% if @follow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@follow.errors.count, "error") %> prohibited this follow from being saved:</h2>

      <ul>
      <% @follow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :followable_id %><br>
    <%= f.number_field :followable_id %>
  </div>
  <div class="field">
    <%= f.label :followable_type %><br>
    <%= f.text_field :followable_type %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%#= f.text_field :user_id %>
    <%=f.collection_select :user_id, User.order(:email),:id,:email,include_blank: true%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
